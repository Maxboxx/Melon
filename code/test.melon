
include Math.Vector

Vector?: v = Vector(1, 2, 3)

if Vector: v = v then
	int?: m = v.y + 2

	guard int: a = m
	
	a = a + 1
end

while Vector: i = v do
	i.z = i.z - 1
	
	if i.z > 0 then
		v!.z = i.z -- warning
	else
		v = nil
	end
end
